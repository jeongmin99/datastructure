linked list에서 first노드(헤더노드) 값을 이동하려는 시도를 하면 안된다. 헤더노드는 항상 고정되어야 한다.
first->link 는 틀림

doubly에서도 newNode->rlink=h->first;
		         h->first->llink=newNode;
		          h->first=newNode; 로 쓰면 안되고
              
              newNode->rlink=h->first;
		          newNode->rlink->llink=newNode;
		          h->first=newNode;로 쓰는 것이 맞다
